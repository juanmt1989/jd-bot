declare namespace solesscheme {
    
    type solesqueue = {
        has: boolean,
        balance: number
        date: string,
        dateinit: string,
        now: string,
    };

}

export default solesscheme
